You are the sole provider writer for RC4 qualification row Q6. Work only in the current isolated
fixture. The outer IIS-0009 requirements and plan are already approved; do not open a nested plan,
ask for approval, or wait for user input.

First invoke `/ifl-ios-standards:brain-flow`, read the loaded plugin's `VERSION`, and confirm it is
`1.0.0-rc.4`. Use only that plugin's Standards. Do not invoke any non-RC4 plugin, MCP, hook, or workflow.
This isolated fixture has no CodeGraph index; do not initialize one—continue with bounded
repository-native discovery for this fixed qualification task.

This is Core + Boardy/VIP + mixed UIKit/SwiftUI + applicable enterprise concerns on an organization
Bazel graph. Starting from exact HEAD `6296c186812011be89e25429f387064e9dedc4a4`, migrate the portable
root bindings from template `2.2.0` to `2.5.0`, reconcile local full-auto authority, then make one
bounded public-contract purity correction in the selected Boardy/UIKit feature. Consider existing
SwiftUI, privacy, operability, performance/resilience, testing, and supply-chain/legal surfaces only to
the extent the change impacts them. Do not invent or accept organization policy values.

Keep Boardy in its selected outer shell and Domain/Application framework-neutral. Views render and
forward intent only; Presenter/equivalent owns display-ready, computed, and formatted values. Exercise
provider-native delegation/handoff/resume and one-writer ownership inside this session. Use TDD for the
code correction. Run exactly one smallest viable final repository-owned Bazel signal after the complete
implementation; if it fails, report without rerunning the unchanged signal.

You have scoped authority to edit and make semantic local fixture commits for this Q6 task despite
older project text asking for separate commit approval. Stage only explicit changed paths. Do not push,
tag, publish, install, modify remotes/provider config, or touch paths outside the fixture. Finish with a
clean worktree when possible. Report only the bounded result fields required by
`CLAUDE-CLI-RUNBOOK.md`.
