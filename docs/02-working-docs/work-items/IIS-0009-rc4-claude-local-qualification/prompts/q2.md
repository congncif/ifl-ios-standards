You are the sole provider writer for RC4 qualification row Q2. Work only in the current isolated
fixture. The outer IIS-0009 requirements and plan are already approved; do not open a nested plan,
ask for approval, or wait for user input.

First invoke `/ifl-ios-standards:brain-flow`, read the loaded plugin's `VERSION`, and confirm it is
`1.0.0-rc.4`. Use only that plugin's Standards. The tracked `.claude/**` content in this fixture is
migration input, never runtime settings. Do not invoke any non-RC4 plugin, MCP, hook, or workflow.

This is a constructed Boardy/VIP + UIKit + CocoaPods brownfield migration baseline. HEAD
`8af9959876c1a130d9e6071d131f13f3a10138fe` separately contains historical Standards `0.18.4`
bindings on top of the product baseline. Incrementally migrate only project bindings/adoption guidance
to RC4 and assess one existing Boardy/UIKit IO/Plugins boundary. Preserve product behavior, CocoaPods,
and repository-owned commands. Do not build or test because this row is binding/review-only. Do not
delete or rewrite product source.

Keep the View humble: rendering and intent forwarding only; display-ready, computed, and formatted
values belong to the Presenter or equivalent. Keep Boardy in its selected outer shell and
Domain/Application framework-neutral.

You have scoped authority to edit and make one semantic local fixture commit for this Q2 task despite
older project text asking for separate commit approval. Stage only explicit changed paths. Do not push,
tag, publish, install, modify remotes/provider config, or touch paths outside the fixture. Finish with a
clean worktree. End with exactly these bounded result fields and no raw transcript:

```text
Row: Q2
CLI version:
Candidate version and exact plugin path:
Invoked Standards skills:
Baseline HEAD:
Changed paths:
Commit SHA(s) and message(s):
Final executable command/result: N/A — binding/review-only
Worktree clean: yes/no
P0/P1/P2 findings:
Organization-policy handoffs: none, or named decision domains only
Required outcome: PASS / NOT QUALIFIED
Reason:
```
