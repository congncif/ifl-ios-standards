You are the sole provider writer for RC4 qualification row Q4. Work only in the current isolated
fixture. The outer IIS-0009 requirements and plan are already approved; do not open a nested plan,
ask for approval, or wait for user input.

First invoke `/ifl-ios-standards:brain-flow`, read the loaded plugin's `VERSION`, and confirm it is
`1.0.0-rc.4`. Use only that plugin's Standards. Do not invoke any non-RC4 plugin, MCP, hook, or workflow.
This isolated fixture has no CodeGraph index; do not initialize one—continue with bounded
repository-native discovery for this fixed qualification task.

This is Core + UIKit + SwiftUI on a Bazel brownfield surface, with Boardy explicitly non-applicable.
Starting from exact HEAD `6296c186812011be89e25429f387064e9dedc4a4`, implement one small
framework-neutral widget-search destination policy consumed by the existing SwiftUI widget adapter and
UIKit search adapter. Add the narrowest focused tests or existing executable coverage. Preserve Bazel
and repository-owned commands; introduce no Boardy assumption, package-manager rewrite, broad refactor,
or unrelated cleanup.

Views remain humble: render display-ready state and forward intent only. Computed/formatted display
values live in Presenter/equivalent, while the destination decision remains pure and testable. Use TDD
for the code change, but run exactly one smallest viable final repository-owned Bazel signal after the
complete implementation. If it fails, report it without rerunning the unchanged signal.

You have scoped authority to edit and make semantic local fixture commits for this Q4 task despite
older project text asking for separate commit approval. Stage only explicit changed paths. Do not push,
tag, publish, install, modify remotes/provider config, or touch paths outside the fixture. Finish with a
clean worktree when possible. Report only the bounded result fields required by
`CLAUDE-CLI-RUNBOOK.md`.
