#!/bin/bash
set -euo pipefail

if [ "$#" -lt 1 ] || [ -z "$1" ]; then
    echo "ifl-tooling-run: missing product" >&2
    exit 2
fi

SCRIPT_DIR="$(cd -P "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PRODUCT="$1"
shift

exec "$SCRIPT_DIR/ifl-tooling-swift" run "$PRODUCT" "$@"
